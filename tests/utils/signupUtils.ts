export const getImieLocator = (page) => page.getByLabel("Imię:");
export const getEmailLocator = (page) => page.getByLabel("Email:");
export const getPasswordLocator = (page) => page.getByLabel("Hasło:");
